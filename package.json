{
  "scripts": {
    "build": "npm-run-all transpile typecheck",
    "transpile": "babel src -d lib",
    "typecheck": "tsc"
  },
  "devDependencies": {
    "babel-cli": "latest",
    "babel-preset-typescript": "7.0.0-alpha.7",
    "npm-run-all": "^4.0.2",
    "typescript": "next"
  }
}
